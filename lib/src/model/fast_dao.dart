// Technically useless, but implementing it imports the database feature file
interface class FastDao {}

// Technically useless, but implementing it imports the database feature file
interface class FastQueryable {}
